{
    "name": "web_chat",
    "private": true,
    "scripts": {
        "start": "concurrently \"npm:server\" \"npm:client\"",
        "server": "cd server && node server.js",
        "client": "cd client && npm run dev",
        "build": "node build.js"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
